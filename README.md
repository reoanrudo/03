# Air Guitar Pro

エアギターシミュレーションアプリ。カメラで手の動きを追跡し、スマホでフレットを押して演奏するリアルタイムゲーム。

![Version](https://img.shields.io/badge/version-0.0.0-blue)
![License](https://img.shields.io/badge/license-MIT-green)

---

## 概要

Air Guitar Proは、React + TypeScript + Tone.js + TensorFlow.jsを使用したエアギターシミュレーションアプリです。PCとスマホを2デバイスで使用して、リアルタイムでエアギターを演奏できます。

### 主な機能

- 🎸 リアルタイム演奏シミュレーション
- 🤖 ハンドトラッキング（TensorFlow.js HandPose）
- 🎵 リアルなギターサウンド（Tone.js）
- 📱 WebRTCによるP2P通信
- 🎯 スコアシステムとコンボ

---

## スクリーンショット

> **[スクリーンショットの説明]**<br/>
> スクリーンショットを追加してください。

---

## デモ

https://air-guitar-pro-demo.example.com

---

## 技術スタック

| レイヤー | 技術 |
|----------|--------|
| フロントエンド | React 19 + TypeScript + Vite |
| オーディオ | Tone.js (シンセサイザー) |
| ビジョン | TensorFlow.js HandPose |
| 通信 | WebRTC (peer-to-peer) + WebSocket シグナリング |
| バックエンド | FastAPI + Uvicorn |

---

## インストール

### 前提条件

- Node.js >= 20
- Python >= 3.13
- npm または pnpm
- Git

### フロントエンドのセットアップ

```bash
cd frontend

# 依存関係のインストール
npm install

# 開発サーバーの起動
npm run dev
```

### バックエンドのセットアップ

```bash
cd backend

# 依存関係のインストール
pip install -r requirements.txt

# 開発サーバーの起動
uvicorn main:app --reload
```

---

## 使用方法

### 基本的な使用フロー

1. **PCとスマホの準備**
   - PCで`PC MODE`を選択
   - スマホで`MOBILE MODE`を選択
   - 両方で同じルームIDを入力

2. **演奏開始**
   - カメラの許可をリクエスト
   - スマホでフレットを押す
   - PCでカメラの前で指を振って演奏

3. **スコアを獲得**
   - ノーツをタイミングよく弾く
   - コンボを繋げる
   - 高スコアを目指す

---

## プロジェクト構造

```
air_guitar_03/
├── backend/               # FastAPI WebSocket シグナリングサーバー
│   ├── main.py
│   └── requirements.txt
├── frontend/              # React + TypeScript アプリケーション
│   ├── components/       # React コンポーネント
│   │   ├── Lobby.tsx
│   │   ├── PCPlayer.tsx
│   │   └── MobileController.tsx
│   ├── services/         # サービスクラス
│   │   ├── WebRTCService.ts
│   │   └── AudioEngine.ts
│   ├── components/__tests__/  # テストファイル
│   ├── types.ts
│   ├── vite.config.ts
│   ├── tsconfig.json
│   ├── eslint.config.js
│   ├── .prettierrc.json
│   ├── knip.config.ts
│   ├── vitest.config.ts
│   └── package.json
├── spec/plan/            # 設計・要件・タスク
│   ├── design.md
│   ├── requirements.md
│   └── tasks.md
└── AGENTS.md             # エージェント開発ガイド
```

---

## 使用可能なコマンド

### フロントエンド (React + TypeScript)

```bash
cd frontend

# 開発サーバー（ポート3000、ホスト0.0.0.0）
npm run dev

# プロダクションビルド
npm run build

# プロダクションビルドのプレビュー
npm run preview

# リント（コード品質チェック）
npm run lint

# フォーマット（コード整形）
npm run format

# 未使用ファイルの検出
npm run knip

# テスト実行
npm run test
npm run test:ui
npm run test:run

# クリーンアップ
npm run clean
```

### バックエンド (FastAPI)

```bash
cd backend

# 依存関係のインストール
pip install -r requirements.txt

# 自動リロード付きで開発
uvicorn main:app --reload

# プロダクション
uvicorn main:app --host 0.0.0.0 --port $PORT

# テスト
pytest
```

---

## テスト

### フロントエンド

```bash
cd frontend
npm run test
```

### バックエンド

```bash
cd backend
pytest
```

---

## CI/CD

GitHub Actionsを使用して、フロントエンドとバックエンドのテストを自動実行しています。

- **Frontend CI**: `.github/workflows/frontend-ci.yml`
- **Backend CI**: `.github/workflows/backend-ci.yml`

---

## コード品質

このプロジェクトでは以下のツールを使用してコード品質を維持しています：

- **ESLint**: JavaScript/TypeScript のリント
- **Prettier**: コードフォーマット
- **Knip**: 未使用ファイルの検出
- **Vitest**: フロントエンドの単体テスト
- **pytest**: バックエンドの単体テスト

---

## 開発ガイド

詳細な開発ガイドは`AGENTS.md`を参照してください。

---

## 制約と注意点

- MobileControllerは4フレットまでしか操作できない
- WebRTCシグナリングサーバーが別途必要
- カメラ許可が必要
- TensorFlow.jsモデルの初期ロードには数秒かかる場合があります

---

## ライセンス

MIT License - 詳細は`LICENSE`ファイルを参照してください。

---

## 貢献

貢献を歓迎します！以下の手順で貢献してください：

1. フォークする
2. 機能ブランチを作成する (`git checkout -b feature/AmazingFeature`)
3. コミットする (`git commit -m 'Add some AmazingFeature'`)
4. プッシュする (`git push origin feature/AmazingFeature`)
5. プルリクエストを作成する

---

## 著者

- [Taguchi Reo] - [GitHub](https://github.com/yourusername)
